#include <iostream>
#include <iomanip>

using namespace std;

int main() {
    int n, count = 0, temp = 0;
    setlocale(LC_ALL, "RUSSIAN");
    cout << "Введите число N (1 <= N <= 1000): ";
    cin >> n;
    cout << endl;
    if (n >= 1 and n <= 1000) {
        for (int i = 1; i < n; i++) {
            for (int y = 1; y < i; y++) {
                if (i % y == 0 and (y >= 10 and y <= 99 and y != i)) {
                    count++;
                }
            }
            if (count != 0) {
                // Выравнивание по правому краю, установление ширины поля отображения для числа
                cout << setiosflags(ios::right) << setw(6) << i;
                temp++;
                if (temp % 10 == 0) {
                    cout << endl;
                    // Перенос строки
                }
                else cout << "\t";
                // Табуляция
            }
            count = 0;
        }
    }
    else cout << "Ошибка! Введено неверное N.";
}
